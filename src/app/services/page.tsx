import type { Metadata } from "next";import SiteHeader from "@/components/SiteHeader";import Footer from "@/components/Footer";import WhatsApp from "@/components/WhatsApp";import ServiceCard from "@/components/ServiceCard";import { services } from "@/data/services";
export const metadata:Metadata={title:"AC Services",description:"Explore split, window, VRV, cassette, ductable, chiller, AHU, FCU and commercial AC services in Gurugram."};
export default function Services(){return <><SiteHeader/><main><section className="page-hero"><div className="container"><span className="eyebrow">Complete cooling care</span><h1>AC services for every space</h1><p>Installation, repair, gas filling and maintenance for home, office and industrial cooling systems.</p></div></section><section className="section"><div className="container service-grid">{services.map(s=><ServiceCard key={s.slug} service={s}/>)}</div></section></main><Footer/><WhatsApp/></>}
