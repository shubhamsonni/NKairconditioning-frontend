import type { Metadata } from "next";import { notFound } from "next/navigation";import Image from "next/image";import Link from "next/link";import { ArrowLeft, Check, Phone } from "lucide-react";import SiteHeader from "@/components/SiteHeader";import Footer from "@/components/Footer";import WhatsApp from "@/components/WhatsApp";import { services } from "@/data/services";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)return{};return{title:s.name,description:`${s.description} Contact N K Airconditioning for ${s.name} service in Gurugram.`,alternates:{canonical:`/services/${s.slug}`},openGraph:{title:`${s.name} Service in Gurugram`,description:s.description,images:[s.image]},twitter:{card:"summary_large_image",title:`${s.name} Service in Gurugram`,description:s.description,images:[s.image]}}}
export default async function Detail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)notFound();return <><SiteHeader/><main><section className="detail-hero"><div className="container detail-hero-grid"><div><Link href="/services" className="back"><ArrowLeft size={17}/> All services</Link><span className="eyebrow">N K Airconditioning</span><h1>{service.name}</h1><p>{service.description}</p><div className="hero-actions"><Link className="btn" href={`/book?service=${service.slug}`}>Get a quote</Link><a className="btn btn-ghost" href="tel:+919466980984"><Phone size={18}/> Call now</a></div></div><div className="detail-cover"><Image src={service.image} alt={service.name} fill priority sizes="50vw"/></div></div></section><section className="section"><div className="container"><div className="section-head"><div><span className="eyebrow dark">Recent work</span><h2>{service.name} gallery</h2><p>Sample images for this category. Your original project photographs can be added here later.</p></div></div><div className="gallery">{service.gallery.map((img,i)=><div key={i}><Image src={img} alt={`${service.name} work ${i+1}`} fill sizes="(max-width:700px) 100vw, 33vw"/></div>)}</div></div></section><section className="section pale"><div className="container included"><div><h2>What we can help with</h2><p>Our team supports both planned work and urgent breakdowns across Gurugram.</p></div><ul><li><Check/> New installation and replacement</li><li><Check/> Fault diagnosis and repair</li><li><Check/> Gas filling and leakage checks</li><li><Check/> Cleaning and preventive maintenance</li></ul></div></section></main><Footer/><WhatsApp/></>}
