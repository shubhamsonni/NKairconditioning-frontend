import type { Metadata } from "next";import {notFound} from "next/navigation";import Link from "next/link";import {ArrowRight,Check,MapPin,Phone} from "lucide-react";import SiteHeader from "@/components/SiteHeader";import Footer from "@/components/Footer";import WhatsApp from "@/components/WhatsApp";import ServiceCard from "@/components/ServiceCard";import {serviceAreas} from "@/data/serviceAreas";import {services} from "@/data/services";
export function generateStaticParams(){return serviceAreas.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const area=serviceAreas.find(a=>a.slug===slug);if(!area)return{};const title=`AC Repair & Service in ${area.name}`;const description=`AC repair, installation, gas filling and maintenance in ${area.name} and nearby areas within 15 km. Call N K Airconditioning.`;return{title,description,keywords:[`AC repair ${area.name}`,`AC service ${area.name}`,`AC installation ${area.name}`,`AC mechanic ${area.name}`],alternates:{canonical:`/locations/${area.slug}`},openGraph:{title,description},twitter:{title,description}}}
export default async function LocationPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const area=serviceAreas.find(a=>a.slug===slug);if(!area)notFound();return <><SiteHeader/><main><section className="location-hero"><div className="container"><span className="eyebrow"><MapPin size={14}/> 15 km service coverage</span><h1>AC repair and service in {area.name}</h1><p>Fast local help for Split, Window, Tower, Cassette, Package, Duct, Chiller, Cold Unit, AHU and FCU air conditioning systems.</p><div className="hero-actions"><a className="btn" href="tel:+919466980984"><Phone/> Call connect</a><a className="btn btn-ghost" href="https://wa.me/919466980984?text=Hello%2C%20I%20need%20AC%20service.%0AName%3A%20%0ACurrent%20location%3A%20%0APhone%20number%3A%20%0AProblem%3A%20" target="_blank" rel="noreferrer">Book on WhatsApp</a></div></div></section><section className="area-assurance"><div className="container"><Check/><div><b>Service available near {area.name}</b><span>We cover addresses within approximately 15 km, subject to final address confirmation.</span></div></div></section><section className="section"><div className="container"><div className="section-head"><div><span className="eyebrow dark">Local AC services</span><h2>AC technicians serving {area.name}</h2><p>Choose your AC type to see repair, installation and maintenance details.</p></div><Link href="/services">View every service <ArrowRight/></Link></div><div className="service-grid">{services.slice(0,9).map(s=><ServiceCard key={s.slug} service={s}/>)}</div></div></section></main><Footer/><WhatsApp/></>}
