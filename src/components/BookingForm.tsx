"use client";import { FormEvent,useState } from "react";import { services } from "@/data/services";
export default function BookingForm(){const [status,setStatus]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Sending…");const form=new FormData(e.currentTarget);const data=Object.fromEntries(form.entries());try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:5000"}/api/bookings`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!r.ok)throw new Error();setStatus("Thank you! We’ll call you shortly.");e.currentTarget.reset()}catch{setStatus("The online form is not connected yet. Please call +91 94669 80984.")}}return <form className="booking-form" onSubmit={submit}><div className="field-row"><label>Name<input name="name" required placeholder="Your full name"/></label><label>Phone<input name="phone" required type="tel" placeholder="10-digit mobile number"/></label></div><div className="field-row"><label>Email (optional)<input name="email" type="email" placeholder="you@example.com"/></label><label>Service<select name="service" required defaultValue=""><option value="" disabled>Select a service</option>{services.map(s=><option key={s.slug}>{s.name}</option>)}<option>AC on rent</option></select></label></div><div className="field-row"><label>Preferred date<input name="preferredDate" type="date"/></label><label>Location<input name="location" placeholder="Area / Sector"/></label></div><label>Tell us what you need<textarea name="message" rows={4} placeholder="AC type, issue, quantity or any other details"/></label><button className="btn" type="submit">Request a callback</button>{status&&<p className="form-status" role="status">{status}</p>}</form>}
