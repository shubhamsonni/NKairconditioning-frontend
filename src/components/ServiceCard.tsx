import Image from "next/image";import Link from "next/link";import { ArrowUpRight } from "lucide-react";import type { Service } from "@/data/services";
export default function ServiceCard({service}: {service:Service}){return <article className="service-card"><Link href={`/services/${service.slug}`} className="service-image"><Image src={service.image} alt={`${service.name} service`} fill sizes="(max-width: 700px) 100vw, 33vw"/></Link><div className="service-card-body"><span>Installation • Repair • Maintenance</span><h3>{service.name}</h3><p>{service.description}</p><Link href={`/services/${service.slug}`}>View work & details <ArrowUpRight size={17}/></Link></div></article>}
